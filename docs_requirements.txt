# Documentation build requirements
# Used to build documentation from reStructuredText (.rst) with Sphinx

# Sphinx and theme
sphinx>=5.0,<8
sphinx-rtd-theme>=2.0
sphinx-copybutton>=0.5

# Optional: regenerate RST from legacy SRT (run srt_to_rst.py)
# pathlib2>=2.3.0; python_version < "3.4"
